
service: my-express-application

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: us-east-1

functions:
  index:
    handler: handler.index
    events:
      - http: 
          path: hello
          method: get
          cors: true

